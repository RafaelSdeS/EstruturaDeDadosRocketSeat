// Imperativo

let number = 2
function square() {
  return number * number
}
number = square()

// Declarativo
const square = n => n * n //Continua sendo uma função. square(n) para tirar o valor
